export interface Reunion {
    data: {
        id: number,
        tema: string,
        area: string,
        fecha: string,
        desde: string,
        hasta: string,
        lugar: string,
    }
}